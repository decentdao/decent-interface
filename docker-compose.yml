version: '3'
services:
  blockchain:
    build: docker/blockchain
    ports:
      - "8545:8545"
    environment:
      - ALCHEMY_API_KEY

  webapp:
    build: 
      context: ./
      dockerfile: ./docker/webapp/Dockerfile
    ports:
      - "3000:3000"
